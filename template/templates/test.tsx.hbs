import React from 'react';
import { render } from '@testing-library/react';
import { {{ pascalCase name }} } from '.';

describe('When rendering {{ pascalCase name }}', () => {
  it('should render', () => {
    const { getByText } = render(
      <{{ pascalCase name }} />,
    );

    expect(true).toBe(true);
  });
});
