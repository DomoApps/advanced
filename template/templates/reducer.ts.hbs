import { createReducer, PayloadAction } from '@reduxjs/toolkit';
import * as {{ pascalCase name }}Actions from 'actions/{{ lowerCase name }}';
import { RootState } from '..';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface {{ pascalCase name }}State {}

const initialState: {{ pascalCase name }}State = {};

const {{ pascalCase name }}Reducer = createReducer(initialState, ({{ lowerCase name }}) => {
  {{ lowerCase name }}.addDefaultCase((state: {{ pascalCase name }}State) => state);
});

// The function below is called a selector and allows us to select a value from
// the state. Selectors can also be defined inline where they're used instead of
// in the slice file. For example: `useSelector((state: RootState) => state.counter.value)`
// export const selectCount = (state: RootState) => state.counter.value;

export default {{ pascalCase name }}Reducer;
